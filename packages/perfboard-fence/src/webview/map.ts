/**
 * マップの webview のエントリ。**中身は fence-kit にある** — 掴む・置く・消すの
 * 決め事はフェンスの文法を知らないので、3 つのフェンスで同じものを使う
 * (52 の docs/13)。
 *
 * ここに包みを 1 つ置くのは、**ビルドが要るファイルはパッケージの中に置く**
 * という約束のため (リポジトリ直下の CLAUDE.md の約束 2)。
 */
import 'fence-kit/webview';
