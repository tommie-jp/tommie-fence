{
  "name": "copper-fence",
  "displayName": "Copper Fence",
  "description": "Markdown の ```copper フェンスを銅張り基板 (マイクロストリップ・島・CPW) の寸法図としてプレビューする",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "publisher": "tommie",
  "repository": {
    "type": "git",
    "url": "https://github.com/tommie-jp/tommie-fence.git",
    "directory": "packages/copper-fence"
  },
  "type": "module",
  "engines": {
    "vscode": "^1.75.0",
    "node": ">=20"
  },
  "categories": [
    "Programming Languages",
    "Visualization"
  ],
  "keywords": [
    "markdown",
    "microstrip",
    "copper-clad",
    "rf",
    "nanovna",
    "diagram"
  ],
  "exports": {
    "./core": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/core.mjs",
      "require": "./dist/core.cjs"
    },
    "./editor": "./src/core/edit/fenceEditor.ts",
    "./src/core": "./src/core/index.ts",
    "./package.json": "./package.json",
    "./plugin": "./src/markdownItPlugin.ts"
  },
  "bin": {
    "copper-fence": "./dist/cli.cjs"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "types": "node dts.mjs",
    "prepack": "rm -rf dist && node esbuild.mjs --production && npm run types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "check": "npm run typecheck && npm run test",
    "examples": "npm run build && node dist/cli.cjs render examples --out examples/out && node scripts/png.mjs examples/out",
    "schematics": "node ../circuit-fence/dist/cli.cjs render examples --out examples/out/schematic && node ../circuit-fence/scripts/figures.mjs examples examples/out/schematic",
    "docs": "npm run build && node dist/cli.cjs render docs/01-syntax.md --out docs/out"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.1.11",
    "esbuild": "^0.28.2",
    "fence-kit": "*",
    "markdown-it": "^15.0.0",
    "rollup": "^4.63.4",
    "rollup-plugin-dts": "^6.5.1",
    "sharp": "^0.35.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
