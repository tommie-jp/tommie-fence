{
  // webview の中で動くコードだけの設定。**ここだけが DOM を知る。**
  // 根の tsconfig に DOM を足すと、core が DOM を触っても型で気づけなくなる
  // (CLAUDE.md の約束 1: コアは DOM・Node API を使わない)。
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": []
  },
  "include": ["src/webview"],
  // 根の設定の exclude (src/webview) を打ち消す。
  "exclude": []
}
