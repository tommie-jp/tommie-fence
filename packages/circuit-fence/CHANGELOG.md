# Changelog

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [Unreleased]

### Added

- **電流の矢と電圧の符号を書けるようにした**。2 端子部品の行に `i=字` と
  `v=字` を続けて書く (`R: resistor b2 b3 i=i`、`C: capacitor b3 d3 v=vC`)。
  教科書の回路図は、記号よりも「どこを流れる電流を i と呼ぶか」「どちらを +
  と数えるか」を図で決めるので、これが無いと図だけで本文の記号と対応が取れない。
  **向きは先に書いた番地が基準**で、向きのある部品の極性と同じ規則 —
  電流は先の番地から後の番地へ、電圧は先の番地が +。逆にしたいときは番地を
  入れ替える (向きの語を増やして綴りを 2 通りにしない)。字は ID と同じ組み方で
  先頭 1 文字が本体・残りが添字 (`i=i1` は i の添字 1)。使える字と長さの
  上限も値と同じで、図に出る字は値と同じ関門を通る。書く順は決まっていない。
  **`v=` は値とも `i=` とも並べて書けない** — circuitikz が図の同じ側に出して
  字が重なると実機で確認したので、重ねて描かずに行番号つきで返す
  (`i=` と値は別の場所に出るので並べられる)。

- **交点と交点の間に部品を置けるようにした**。`_` で行と列を切って小数を書く
  (`a_1.5` は列が 1 と 2 の間、`a.5_1` は行が a と b の間、2 桁まで)。
  並べた番地の間に後から 1 つ割り込ませても、**後ろの番地を振り直さずに済む**。
  `a1.5` のように `_` で切らない書き方は**わざと通していない** — `.` は足の
  区切りでもあるので (`U1.5` は DIP の 5 番ピン)、切らずに書けるようにすると
  どちらのつもりで書いたのかを読む順で決めることになる。分数 (`a.1/4_2`) と
  `a_1` (小数の無い `_` 付き) も通さない。同じ場所の綴りを 2 通りにすると、
  ネットの名前も図どうしの突き合わせもその分だけ揺れる。書き間違いには
  直せる形を返す (`a1.5` → 「交点の間は `_` で行と列を切ります (`a_1.5`)」)。
  グリッドの点は交点の上にだけ打ち、間に置いた部品はその間に乗る。
  TikZ の座標名からは `.` を外す (`.` はノードの足の区切りなので、名前に
  入れると座標として読まれない)。**交点だけで描いた図の TeX は 1 バイトも
  変わらない**。

- **ライブラリの出口 `circuit-fence/core` を足した**。コア (YAML → 検証 →
  circuitikz TeX) を、サーバーやスクリプトから `npm pack` の tarball 経由で
  読み込める (`exports` に ESM / CJS / 型定義)。中身は同期の純関数のままで、
  描画エンジン (node-tikzjax) はこの出口に含まれない — TeX → SVG は
  呼ぶ側の描画キューにつなぐ (CLI と同じ流れ)。VS Code 拡張の入り口
  (`main` / `browser`) は変わらない。

- **図に題を載せる `title:` を足した**。`title: 回路図01 circuit フェンスの書き方`
  の 1 行で、図の左上に太字で出る。**この 1 つだけは `notes:` の字では置けない** —
  番地は `a1` が最上段で、その上が無いため。置き場所は番地から測らず、TikZ に
  測らせた `current bounding box` の左上に掛ける (ラベルも注釈も刻印も
  描き終わってからでないと、図がどこまで広がるか決まらない)。字はフェンスでは
  TeX に渡さず描き上がった SVG に差し込むので、注釈と同じく日本語が出る。
  大きさ・太さ・色は選べない — 1 枚に 1 つしか無いものなので、選べるようにしても
  覚えることが増えるだけになる。通す字は注釈と同じ関門で、60 文字まで。
  docs と examples の図には `図NN タイトル` の番号も付けてある。文章から
  **「図02 を直して」と指せる**ようにするためのもの。**番号は .md ごとに 01 から
  数え直す** — 通し番号にすると、1 つのファイルに図を足しただけで関係のない
  ファイルまで振り直しになる。docs/01-syntax.md は図を貼らずフェンスを直に書くので、
  20 枚が通しで 図01〜図20 になる (貼ると、その図は元のファイルの番号を名乗って
  通し番号が崩れる)。同じ題の図はどこに書いてあっても中身が同じことと、番号が
  ずれていないことをテストで見張る (ずれても図は描けてしまうので目では気づけない)。
- **バージョンを出せるようにした**。**番号は 1 つだけ**で、構文にも処理系にも
  同じ番号が付く。構文に別の番号を振らないのは、振ると変更のたびに「構文が
  上がるのか処理系だけか」を決める手間が増えるのに、それで良くなるのは
  「新しい構文を古い処理系で開いた」ときの文面だけだから。いまは知らない項目も
  知らない語も行番号つきで返るので、そこは足りている。出し方は 3 つ。
  - `style: stamp: on` — 図の右下に `circuit-fence 0.1.0` と刻む。
    **字は書かせない**。書けるのは出す/出さないだけで、番号は処理系が埋める。
    手で書けるようにすると、更新した瞬間にその字が嘘になる (表示が無いことより
    古い番号が出ているほうが害が大きい)。置き場所は番地から測らず、TikZ に
    測らせた `current bounding box` の右下に掛ける — ラベルも注釈も描き終わる
    まで図がどこまで広がるか決まらないため。グリッドと同じ `gray` で描くので、
    回路より薄く出て `grid-color` で色が変わる。刻印は ASCII だけなので、
    プレビューと `--emit-tex` で同じものが出る。
  - `data-circuit-fence` — **刻印の有無によらず**、書き出した `.svg` の根と
    プレビューの図に必ず入る。図の見た目は変わらない。資料に貼った図が
    どの版で描かれたかを、後から grep でも DOM からでも辿れる。
  - `circuit-fence --version` — 処理系の版を出す。`-v` も同じ。
- **書き出し (`- source`) の行送りを `tight` / `loose` で選べるようにした**。
  書かなければ既定 (その中間)。長いフェンスを図の高さに収めたいときは `tight`、
  1 行ずつ指しながら説明したいときは `loose`。`tight` でも**字の高さは
  下回らない** — それより詰めると上の行の下がりと下の行の上がりが噛む。
  段に `normal` を置かないのは、それが字の大きさの名前として埋まっているため
  (語は 1 つの並びに混ぜて書くので、同じ名前があるとどちらの意味か決められない)。
  行送りは何行も並ぶものにしか意味がないので、`text` や印に書いたら
  行番号つきで「書き出し (source) にだけ書けます」と返る。
- **CLI に `check` を足した**。図を描かず、読めなかった行とネットリストだけを
  出す。WASM の TeX を回さないので `examples` 全部でも 0.1 秒で終わり
  (`render` は 1 枚 1 秒近くかかる)、書きながら回すときと CI で使える。
  見るものは描くときとまったく同じなので、ここで通った図はプレビューでも
  同じことを言われない。読めなかった行があれば 0 以外で終わる。
- **1 画面ぶんの早見表 [docs/02-cheatsheet.md](docs/02-cheatsheet.md) を足した**。
  全部の形と種類・足の名前・よく踏むところを 1 枚に収めてある。
  LLM に書かせるときはこれをそのまま渡す。載っている名前が実装から
  遅れていないかはテストで見張る。
- **`points:` で番地に名前を付けられるようにした** (`fb: d4`)。番地を書ける
  場所ならどこでも名前で書ける (部品の両端・配線の端点・注釈の指し先や位置・
  `grid-to`)。同じ節点を何か所からも指すとき、**動かすときに直すのは 1 行だけ**
  になる。番地を何か所にも書いていると、1 か所だけ直し忘れても図は描けてしまい、
  つながり方が変わったことに気づけない。読んだ直後に番地へ開くので、
  図も TeX も番地で書いたときと 1 バイトも変わらない。
  名前に**番地の形は使えない** (`a1: c5`) — どちらの意味かを読む順で決めることに
  なり、書いた人には見えないため。部品 ID と同じ名前も使えない
  (注釈の指し先がどちらを指すか決まらない)。
  名前の乗った節点は**ネットリストにその名前で出る**。ポートやグラウンドが
  乗っているネットはそちらが勝つ (図に見えている名前のほうが探しやすい)。
- **向きのある 2 端子部品の極性を 1 つの規則にまとめた**。`ecap` / `vsource` /
  `battery` / `solar` / ダイオード各種 / `thyristor` のどれも
  **先に書いた番地が + 側 (アノード)**で、記号ごとの例外はない
  ([docs/01-syntax.md](docs/01-syntax.md) の「向きのある部品は、先に書いた番地が + 側」)。
- **読めてはいるが思ったとおりに出ない書き方を、お知らせで伝えるようにした**。
  図は書いたとおりに描いたうえで、行番号つきで帯に出る (エラーとは分けてある)。
  いまのところ 2 つ。**`--` で足へ引いて斜めに入る**とき — 記号の中心線に出る足
  (トランジスタの C・E、オペアンプの `out` など) へ軸を揃えて引くときは
  何も言わない。中心線は記号ごとに実機の図で確かめて表に載せてある。
  もう 1 つは**注釈の指し先が部品 ID にも番地にも読める**とき — その番地にも
  何か置いてあるときだけ、どちらを取ったかを伝える (行は a〜z あるので ID は
  たいてい番地の形にもなり、空の番地まで言い出すと口を出しすぎる)。
- **配線を 1 行につないで書けるようにした** (`b1 -- b3 |- U1.+`)。端点は 3 つ
  以上並べてよく、演算子は区間ごとに選べる。**1 行が 1 本の信号経路**として
  読めるので、節点を通る道筋を目で追える。読んだ直後に隣どうしの 1 区間ずつへ
  開くので、分岐の黒丸もネットリストも 1 行ずつ書いたときと変わらない。
  読めなかったときに返る行は書いた 1 行のまま (どの区間が悪くてもその行に付く)。
- **よく書く種類を略記でも書けるようにした** (`r` = `resistor`、`gnd` = `ground`、
  `ac` = `sine` など 20 個)。読んだ直後に正式名へ畳むので、図もネットリストも
  エラーの文面も正式名のまま — 略記は**書く手数を減らすためだけ**のもの。
  全部の種類には付けていない。SPICE の素子文字と回路図で通っている略語だけで、
  `q` (npn か pnp か決まらない) のように指すものが 1 つに決まらないものは
  持たせていない。一覧は [docs/01-syntax.md](docs/01-syntax.md) の「略記」。

- **注釈の字に大きさ・寄せ・太字を書けるようにした**。大きさは `tiny` /
  `small` / `normal` / `large` / `huge` の 5 段 (極小から極大まで)。寄せは
  `left` / `center` / `right` で、番地を字の左端・真ん中・右端のどれにするかを
  決める。`bold` で太字。`text` と `source` で同じ言葉が使え、**どの順に書いても
  よい**。pt の直接指定は受け付けない — 色と同じで、実機に通した指定だけを
  名前で引く (プレビューの TeX はフォントが無いと例外ではなくプロセスごと落ちる)。
- **`- box 番地 番地 [色]` で図の一角を枠で囲めるようにした**。2 つの番地が枠の
  対角になり、外側に余白を取るので縁の記号やラベルは枠に噛まない。回路の線と
  見分けが付くよう破線で描く。
- **`- arrow 起点 終点 [色]` で指し棒を引けるようにした**。両端とも部品 ID か
  番地。部品を指した端は印 (`circle`) と同じ丸の縁で止まる (真ん中まで伸ばすと
  先端が記号の下に隠れるため)。起点と終点が同じところだと行番号つきで返る。
- グリッドは**点を薄く、行英字と列数字を濃く**描く。点は位置の目安でしかないが、
  英字と数字は読んで番地を数えるものなので、濃さを分けた。色は 1 つのまま
  (`grid-color` の指定はどちらにも効く) で、点だけを薄めて描いている。
- CLI も読めなかった行を **Markdown の行**で返すようにした。フェンスの中の数え方で
  返していたので、プレビューの帯や図に書き出した番号と食い違っていた。
- **`- source 番地 [色]` で、そのフェンスの中身を図に並べられる**。囲みの ```
  も付く。プレビューではフェンスが図に差し替わって書いた YAML が見えなくなるので、
  図と並べて読めるようにしておくためのもの。中身は書き写すのではなく**フェンス
  自身から作る**ので、図を直すと書き出しも動く。行送りは番地の刻みではなく字の
  高さで決まるので、行数が増えても図ほどは伸びない。
- **`notes:` で図に注釈を重ねられる**。`- circle 部品IDか番地 [色]` で丸を描き、
  `- text 番地 [色や大きさ]: 文字` で好きな場所に字を書く。注釈は**回路の一員ではない**
  ので、ネットリストにも分岐の黒丸にも数えない。色は `red` / `blue` / `green` /
  `orange` の 4 つで、明暗どちらのテーマでも読める値にしてある。
- **注釈の字はプレビューでも日本語が出る**。部品の値と違って、フェンスの TeX に
  字を渡さず (渡すとフォントが無くてプロセスごと落ちる)、置き場所だけを描かせて
  描き上がった SVG に差し込むため。書き出す `.tex` は TeX に組ませるので、
  どちらでも同じ字が出る。値では通さない `:` も注釈だけは通す
  (部品の 1 行をそのまま書き写せるように)。
- ` ```circuit ` フェンス (YAML) が Markdown プレビューで回路図になる。
  番地 (`a1` `c3`) で置く場所を書くと、YAML → circuitikz TeX → WASM の TeX
  → SVG の順に描かれる。LaTeX のインストールは要らない。
- 2 端子部品 40 種。抵抗とその仲間 (`resistor` / `resistor-var` /
  `potentiometer` / `photoresistor` / `thermistor` / `thermistor-ntc` /
  `thermistor-ptc` / `varistor`)、
  コンデンサとコイル (`capacitor` / `ecap` / `varicap` / `inductor`)、
  振動子 (`crystal`)、ダイオードとその仲間 (`diode` / `led` / `zener` /
  `schottky` / `photodiode` / `diac` / `thyristor` / `triac`)、
  電源 (`vsource` / `sine` / `square` / `triangle` / `isource` / `battery` /
  `solar`)、切る・光る・鳴る・測るもの (`switch` / `switch-nc` / `button` /
  `button-nc` / `reed` / `fuse` / `lamp` / `speaker` / `mic` / `ammeter` /
  `voltmeter` / `ohmmeter`)。1 端子の記号は `port` / `ground` / `vcc` / `vee`。
  配線は `--` (2 点をまっすぐ結ぶ)。
- 電源レールの `vcc` / `vee` は、**ID がそのまま図に出て、乗っているネットの
  名前にもなる** (端子と同じ)。グラウンドと違って離して描いても自動では
  つながらない (`5V` と `3V3` を同じ節点にしてしまうため)。
- **2 端子部品でも足を持てる**。ポテンショメータのワイパー (`P1.w`)、
  サイリスタとトライアックのゲート (`T1.g`) は、両端を番地で置いたうえで
  3 本目を名前で指す。足を持つかどうかは種類が多端子かどうかではなく、
  足の表を持っているかで決まる。
- ロジックゲート `and` / `or` / `nand` / `nor` / `xor` / `xnor` (入力は `a` `b`
  または `1` `2`) と、入力 1 本の `not` / `buffer` (`in` / `out`)。
- DIP の IC `dip8` / `dip14` / `dip16` / `dip20` / `dip28` / `dip40`。
  足は番号で指し (`U1.1`)、型番は記号の**中**に出る。
  足の本数はパッケージごとに決まっているので、文法に欄を足さず種類の名前に入れた。
- IGBT の `nigbt` / `pigbt` (足は `G` `C` `E`) と、切り替えスイッチの `spdt`
  (`in` / `1` / `2`)。
- `ecap` (電解コンデンサ) は**先に書いた番地が + 側**。曲板の記号で描くので、
  向きは記号そのものに出る (`+` の字は書き足さない)。
- `:` を引用符なしで書いて YAML がマップとして読んでしまったとき、
  「`:` を含む文字は `"…"` で囲みます」を理由に添える (yaml 自身の言い分は
  英語で「Nested mappings are not allowed」とだけなので、直し方が分からない)。
- 種類を書き間違えたとき、近い名前があれば候補を添える
  (`resistr` → 「resistor のことですか?」)。全部を並べるより読みやすい。
- 多端子部品: `npn` / `pnp` / `nmos` / `pmos` / `opamp` / `transformer`。
  1 つの番地に置き、足は `Q1.B` `U1.out` のように名前で指す
  (`Q1.base` のように綴っても同じ)。番地の後ろに書いた型番は記号の下に出る。
  オペアンプは `+up` で ± の上下を選べる。記号の ± は字ではなく線で描く
  (フェンス側のフォントでは字が壊れるか、+ と釣り合わないため)。
- FET は簡易記号 (`nmos` / `pmos`) のほかに、接合型 (`njfet` / `pjfet`)、
  エンハンスメント型 (`nmos-e` / `pmos-e`)、デプレッション型
  (`nmos-d` / `pmos-d`) を書き分けられる。**足の名前はどれも同じ**なので、
  記号だけ後から差し替えられる。
- 配線の折れ。`-|` は先に横、`|-` は先に縦 (TikZ と同じ)。
- 端が別の配線の**途中**に乗っているところ (T 字) はつながっているものとして
  扱い、黒丸を打ってネットリストでも 1 つにまとめる。十字に通り過ぎるだけは
  つながらない。ただし足へ引いた線の上は判定できないので、その旨を伝える。
- **部品も配線も斜めに置ける**。行も列も揃っていない 2 点の間にそのまま引く。
- 端が 3 つ以上集まる交点に**分岐の黒丸**を自動で打つ。
- 同じ場所に置かれた部品を**行番号つきで返す**。図には両方とも描く
  (どちらが間違いかは書いた人にしか分からない)。
- 部品 ID は回路図の慣習どおり組む (`R1` → R の添字 1)。
  値は種類から単位を補う (抵抗の `10k` → 10 kΩ)。
- **ネットリスト**を図の下に畳んで出す。ポートが乗っているネットはポート名、
  グラウンドが乗っていれば `GND`。グラウンド記号は離して描いても同じ節点。
- 読めなかった行を **Markdown の行番号つき**で図の下に出す。読めた部品は描く。
  図がまったく組めなかったときは理由だけのカードを出す。
  1 行の頭には `circuit:` を付ける (`circuit: 7 行目: 種類 resistr は知りません`)。
  プレビューの帯でも CLI の標準エラーでも同じ形なので、
  ほかのツールの出力に混ざっても出どころが分かる。
- CLI (`circuit-fence render`)。1 枚につき `.tex` と `.svg` を書き出す。
  `.tex` は `\documentclass{standalone}` から始まる、LaTeX にそのまま渡せる原稿。
- **`--emit-tex`** — 図を描かず、手元の xelatex 用の `.tex` だけを書き出す。
  フェンスとの違いは**フェンス側の制約が強いる 3 点だけ**。
  - 日本語の値が通る (フェンスはフォントが無く、行番号つきで返す)
  - 単位を siunitx で組む (`100 uF` ではなく `100 µF`)
  - オペアンプが本物の `op amp` になる (フェンスは三角形 + 手描きの ±)

  番地も配線も黒丸も同じなので、プレビューで位置を確かめてから書き出せる。
  日本語のフォントは `\newfontfamily` の 1 行にあり、手元に無ければそこを
  書き換える。値が全部 ASCII のときはその行を書かない。
  値に書ける字は `--emit-tex` で日本語と `µ` `Ω` `°` が増えるだけ
  (`\` `$` `,` `=` はどちらでも書けない)。
  プレビュー用と書き出し用の `.tex` は同じ名前になるので、片方の上に
  もう片方を書こうとしたら、書かずに知らせて止まる。
- フェンス内 YAML の色分け (TextMate injection)。
- **`style:`** で見た目を指定できる。テーマだけなら `style: dark` の 1 行でよい。
  - `grid: on` — **部品を置ける位置**を点で出す。行は左に英字、列は上に数字で、
    ブレッドボードと同じ読み方。`grid-to: e12` で使っていない範囲まで伸ばせる
    (部品を動かす先が見える)。
  - `theme` — `auto` (既定) / `light` / `dark` / `mono`。既定はエディタ追従で、
    明るいテーマでも暗いテーマでも読める。**テーマを変えても図は描き直さない**
    (描き上がった SVG の色を差し替えるだけ)。
  - `ink-color` / `paper-color` / `grid-color` — テーマの色を 1 つずつ上書きする。
  - `pitch` / `standard` / `wire-width` — 1 マスの大きさ、記号の流儀
    (`american` / `european`)、線の太さ。
  - `width` — 出力の横ドット数。図の中身は動かさず外寸だけ変える。

### Changed

- **注釈の既定の大きさを `\footnotesize` (8pt) から `\large` (12pt) へ上げた**。
  図を貼って読むとき 8pt では小さく、書き手がほとんどの注釈に `large` と書き
  足していた。**書かないときに欲しい大きさ**が既定であるべきなので、5 段ごと
  1.5 倍ずらして名前と見た目の対応を保った (`tiny` 5→8、`small` 7→10、
  `normal` 8→12、`large` 12→17.28、`huge` 17.28→24.88pt)。極小は既定の 2/3、
  極大は約 2 倍、という比は動いていない。段はどれも実機に通して確かめてある。
  docs と examples から `large` の書き足しを外した (新しい既定と同じ大きさ)。
  プレビューは「`normal` が地の文と同じ大きさ」で倍率を決めるので、図そのものは
  地の文に対して 1.5 倍細かく出る (注釈が回路の記号より大きく見えるようになる)。
- **`theme: mono` が注釈の色も潰すようにした**。「黒一色」と説明しているのに
  注釈 (`red` / `blue` / `green` / `orange`) だけ色が残っていた。資料に貼る・
  印刷するためのテーマなので、色が漏れると困る。図は 1 回描いてどのテーマでも
  使い回す作りなので、TeX に焼き込まれた注釈の色も描き上がった SVG で塗り替える。
  色を使いたい図では `mono` を選ばない、という分け方にした。
- **プレビューの図の大きさを、読み手の地の文に合わせるようにした**。
  注釈の `normal` がちょうど地の文と同じ大きさになる倍率で外寸を em で書く
  (`markdown.preview.fontSize` を変えても拡大しても付いてくる)。それまでは
  エンジンが書いたドット数のまま出していたので、書き出しや注釈が周りの文章より
  小さかった (`normal` は 8pt = 10.7 ドット、プレビューの地の文は既定 14 ドット)。
  **合わせる層を表示に置いた**のは、TeX の側では合わせようがないため — 指定を
  いくつにしても、読み手が字の大きさを変えれば同じだけずれる。
  `width` を書いた図は書いたとおりのドット数のままで、CLI が書き出す SVG も
  素の大きさのまま (貼り先の字の大きさをこちらから決めない)。
  viewBox は触らないので図の中身は動かない。
- **書き出し (`- source`) の行送りを詰めた**。それまでは字の注釈と同じ 1.4 em で
  送っていたが、これは図の中に 1 行だけ置く字に合わせた値。書き出しは何行も
  続けて並ぶので、そのまま送ると行の間だけが目立って**まとまりとして読めない**。
  1.15 em にした (1 em を割ると上の行の下がりと下の行の上がりが噛むので、
  そこまでは詰めない)。字の上下に取る余白は変えていない — 余白は図の縁で字が
  切れないための実測値で、行の間隔とは別のもの。

### Notes

- 図は拡張ホスト側で非同期に描き、markdown-it は同期でキャッシュを引く。
  描き終わるとプレビューが描き直される。VS Code のプレビューの CSP は
  `wasm-unsafe-eval` を通さないため、webview の中では WASM を動かせない。
- web 版 (vscode.dev / github.dev) では図を描けない。
  検証・ネットリスト・行番号つきエラーはそのまま使える。
- 日本語の値は、フェンスの TeX にフォントが無いため描けない。行番号つきで返し、
  `--emit-tex` へ誘導する。部品 ID は配線とネットリストで指す名前なので、
  日本語にはできない (英数字と `_` `-` のみ)。
- 記号は 1 つずつ実機に通してから表に載せている。そのままでは使えず、
  置き換えたものが 11 種類。
  - `V` / `sV` / `sqV` / `vsourcetri` (丸い電源) — 落ちないが、circuitikz が
    丸の中身を**縦置き前提で 90 度回して**描く。丸だけの `esource` にして、
    中身 (+ と −、波形) は自分で描いている
  - `ammeter` / `voltmeter` / `ohmmeter` (計器) — 回路図の慣習は丸に字だけ。
    circuitikz の電流計・電圧計は指針の矢が入り、抵抗計は Ω が**太字の数式**で
    そのフォントが無くて**落ちる**。矢の無い `rmeter` に普通の太さの字を渡して
    3 つ揃えた (普通の太さの Ω は出る。値のラベルの Ω と同じもの)
  - `transformer` (トランス) — circuitikz の既定は**空芯**。鉄芯の 2 本が入る
    `transformer core` にした。アンカーは同じなので足の指し方は変わらない
  - `vR` (可変抵抗) — 矢先の向きが circuitikz の版で違う。フェンスの 1.0 だけ
    左下を向くので、そちらでだけ上下を返して右上に揃えた
  - `eC` (電解コンデンサ) — フォントが無く**プロセスごと落ちる**。
    曲板の `cC` で描く (`ecap`)
  - `op amp` — 記号の中の小さな ± のフォントが無くて落ちる。三角形だけの
    `plain amp` + 線で描いた ± にした
  - `thRn` / `thRp` (NTC / PTC サーミスタ) — 落ちないが、中の θ が小さすぎて
    字形が無く `#` で出る。素のサーミスタの記号にして、区別は ID の下に
    `NTC` / `PTC` と書く

### Fixed

- **グラウンドの記号の横棒が潰れて 1 つの塊に見えていた**。3 本の横棒の間隔は
  記号の側で決め打ちなのに、棒の太さは線の太さに付いてくる。既定の 0.8pt では
  隙間が棒の 1/4 (0.39pt) しか残らず、線が重なって見えていた。棒の間に白が残る
  大きさまで記号を広げる (`grounds/scale`)。線が細いときは記号の既定のまま。
  `wire-width` を上限の 4pt にしても潰れないことを実機で確かめてある。
  プレビューと書き出す `.tex` の両方に同じ指定を出すので、出る図は変わらない。
- **太陽電池 (`solar`) の + が、ほかの部品と逆の端に出ていた**。circuitikz は
  `pvsource` を電池と逆向きに描く (先に書いた番地が − 側)。極性のあるほかの
  部品はどれも先に書いた番地が + なので、記号の向きを揃えた。フェンスの
  circuitikz 1.0 でも手元の 1.6.6 でも同じ向きだったので、版差ではなく
  記号の性質として**両方の的に同じ指定を出す**。
- **端点を書き忘れた配線 (`a3 --`) に、空の字を「番地の形ではありません」と
  返していた**。どこが空なのか読めないので、配線の書き方のほうを返す。
- **`ink-color: #333` に「#rgb か #rrggbb で書きます」と返していた**。
  YAML は `#` から先をコメントとして落とすので値が空で届いており、
  そう書いた本人には堂々巡りになる。`"…"` で囲むことのほうを伝える。
- **計器 3 つ (`ammeter` / `voltmeter` / `ohmmeter`) を丸 + 字だけで描くようにした**。
  circuitikz の電流計・電圧計は丸に指針の矢が重なり、字も太字で、回路図で
  よく見る形と違っていた。抵抗計と同じ矢の無い記号に字を渡して 3 つ揃えた。
- **トランスを鉄芯つきの記号にした**。circuitikz の `transformer` は空芯
  (巻線 2 つだけ) で、記事によく出るのは鉄芯の 2 本が入るほう。
  アンカーは同じなので `T1.a1` などの指し方は変わらない。
- **可変抵抗の矢が左下を向いていた**。回路図の慣習は右上。フェンスの
  circuitikz 1.0 だけが左下に描く (手元の LaTeX 1.6.6 は右上) ので、
  **プレビュー側でだけ上下を返して**両方の向きを揃えた。
- **丸い電源 (`vsource` / `sine` / `square` / `triangle`) を横に引くと、記号の
  中身が寝ていた**。直流電源の − が縦棒になり、正弦波・方形波・三角波も縦向きに
  描かれていた。circuitikz が丸の中身を縦置き前提で 90 度回して描いているため
  (手元の LaTeX でも同じ)。丸だけの記号に置き換えて中身は自分で描くようにし、
  **番地の向きによらず一般的な記号の形で出る**ようにした。
  波形は図に対していつも水平で、+ と − だけが配線の向きに沿って並ぶ
  (先に書いた番地が + 側)。書き出す `.tex` も同じ形になる。
- 注釈の色の名前を引くとき、`toString` のように Object.prototype にある名前が
  色として通ってしまっていた。図を描く手前で例外になり、行番号を返せない
  (約束 5) ので、表は自分の持ちものだけを見るようにした。
