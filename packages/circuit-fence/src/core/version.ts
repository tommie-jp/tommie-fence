/**
 * 処理系のバージョン。
 *
 * 持ち主は `package.json` で、ここはその写し。core は Node を使えないので
 * (CLAUDE.md 設計上の約束 1) 読みに行けず、定数で持つほかない。
 * **写しがずれると図に古い番号が焼き付く**ので、ずれていないかは
 * version.test.ts が見張る。
 *
 * **構文の仕様には別の番号を振らない**。振ると変更のたびに「仕様が上がるのか
 * 処理系だけか」を判定する手間が生まれるのに、それで良くなるのは
 * 「新しい構文を古い処理系で開いた」ときのエラーの文面だけ。
 * いまは知らないキーも知らない語も行番号つきで返るので、そこは足りている。
 * 構文が変わったことは semver の minor と CHANGELOG が表す。
 */
export const VERSION = '0.2.0';

/**
 * 図の隅に刻む字 (`style: stamp: on`)。名前まで書く。
 * 番号だけでは、何の 0.1.0 なのかが図から離れると分からない。
 */
export const STAMP_TEXT = `circuit-fence ${VERSION}`;
