/**
 * カーソルや打鍵を追うのをまとめる待ち時間。**マウスで文字を選ぶと 1 秒に何十回も来る** —
 * 1 回ごとに組み直すと、その間ずっと拡張ホストが埋まり、マップの操作が待たされる
 * (52 の docs/27)。人が「ついてこない」と感じない範囲でまとめる。
 *
 * マップのカーソル追随 (`vscodeHost.ts`) と、題の釦の文脈の鍵 (`context.ts`) が使う。
 */
export const FOLLOW_DELAY_MS = 50;
