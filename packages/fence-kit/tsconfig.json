{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "skipLibCheck": true,
    "noEmit": true,
    // fence-kit 本体は Node の API を使わない (使う側がブラウザでも動くため)。
    // それを見張るのは `types` ではなく `src/purity.test.ts` — テスト自身が
    // ファイルを読むのに node:fs を使うので、型だけ外すと通らなくなる。
    "types": ["node"]
  },
  "include": ["src", "vitest.config.ts"],
  // DOM を知るのは webview だけ (下の tsconfig.webview.json が見る)。
  // **DOM を触るファイルだけ**を外す — 同じ webview の下でも `mapState.ts` と
  // `previewGate.ts` は DOM を知らないので、こちらでも見張る。
  "exclude": [
    "src/editor/webview/map.ts",
    "src/editor/webview/domFixture.ts",
    "src/editor/webview/map.dom.test.ts",
    "src/editor/webview/map.chrome.test.ts"
  ]
}
